# 🔥 RAG Pipeline

## Indexing Flow

Document → OCR → Text → Chunking → Embeddings → Vector DB

## Query Flow

User Query → Embedding → Similarity Search → Context Retrieval → LLM → Answer

## Notes

- Top-K retrieval used
- Context window optimized
- Reduces hallucination